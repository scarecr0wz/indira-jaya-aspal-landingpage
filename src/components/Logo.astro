---
import logoImage from '../assets/images/indira-aspal.png';
import { site } from '../data/site';

interface Props {
  light?: boolean;
}

const { light = false } = Astro.props;
---

<a class:list={['logo', { 'logo--light': light }]} href="/#beranda" aria-label={site.name + ' — kembali ke beranda'}>
  <span class="logo__mark" aria-hidden="true">
    <img src={logoImage.src} alt="" />
  </span>
  <span class="logo__text"><strong>{site.name}</strong><small>Spesialis Aspal Hotmix dan Jalan</small></span>
</a>

<style>
  .logo { --logo-cutout: var(--red); display: inline-flex; align-items: center; gap: .68rem; color: var(--ink); text-decoration: none; }
  .logo--light { color: white; }
  .logo__mark { display: grid; width: 2.65rem; height: 2.65rem; place-items: center; overflow: hidden; }
  .logo__mark img { width: 100%; height: 100%; object-fit: contain; }
  .logo__text { display: grid; line-height: 1; }
  .logo__text strong { font-size: .95rem; letter-spacing: -.01em; text-transform: uppercase; }
  .logo__text small { margin-top: .32rem; color: var(--muted); font-size: .57rem; font-weight: 700; letter-spacing: .11em; text-transform: uppercase; }
  .logo--light .logo__text small { color: #aeb1b5; }
</style>
